export interface AmmoTicket {
    cal: string,
    brand?: string,
    bulletWeight?: number,
    bulletType?: string,
    powderWeight?: number,
    powderBrand?: string,
    primerBrand?: string,
    rounds: number
}
